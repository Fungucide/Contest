#include <stdio.h>
#include <list>
#define scan(x) do{while((x=getchar())<'0'); for(x-='0'; '0'<=(_=getchar()); x=(x<<3)+(x<<1)+_-'0');}while(0)
char _;

using namespace std;

int N,Q;
list<pair<int,int>> revealed[2];

int main() {
	scan(N);scan(Q);
	int com, a, b, c;
	for (int i = 0;i < Q;i++) {
		scan(com);
		if (com == 1) {

		}
		else if (com == 2) {
		
		}
	}

}