#include <stdio.h>
#include <set>
#define scan(x) do{while((x=getchar())<'0'); for(x-='0'; '0'<=(_=getchar()); x=(x<<3)+(x<<1)+_-'0');}while(0)
char _;
#define MAXN 100001

using namespace std;

ordered_set<int> x;
int N, M;

int main() {
	scan(N);scan(M);

}